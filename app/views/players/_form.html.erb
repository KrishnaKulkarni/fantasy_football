<%= form_for(player) do |f| %>
  <% if player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :position %>
    <%= f.number_field :position %>
  </div>

  <div class="field">
    <%= f.label :private_value %>
    <%= f.text_field :private_value %>
  </div>

  <div class="field">
    <%= f.label :public_value %>
    <%= f.text_field :public_value %>
  </div>

  <div class="field">
    <%= f.label :roster_id %>
    <%= f.text_field :roster_id %>
  </div>

  <div class="field">
    <%= f.label :league_id %>
    <%= f.text_field :league_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
